import { Server, Socket } from "socket.io";

export const handleConnection = async (socket: Socket) => {
  //const socketUser = socket.data.user as UserDocument;
  //   const currentUser = (await User.findById(socketUser.id)
  //   .exec()) as UserDocument;
  //   ioSendSocketResponse(
  //     io,
  //     socket,
  //     "Connected: " + (socket.data.user as UserDocument)?.id
  //   );
};
